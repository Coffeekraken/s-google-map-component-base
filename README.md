# Base class for google map based components.

Provide a base component class for google map based components that will handle google api loading and mount the component only when all dependencies are ok.
